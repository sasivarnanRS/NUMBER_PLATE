# Cyberpunk Registration API

Backend for the futuristic vehicle registration system.

## Setup
1. `npm install`
2. Create `.env` file with `MONGODB_URI`
3. `npm run dev`

## Structure
- `src/index.js`: Main server file
- `src/data.js`: Mock data (if applicable)
